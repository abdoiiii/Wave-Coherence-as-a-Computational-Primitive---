[package]
name = "harmonic-transformer"
version = "0.1.0"
edition = "2024"
description = "Character-level harmonic transformer in pure Rust — no tokens, no Python"

[dependencies]
candle-core = "0.8"
candle-nn = "0.8"
rand = "0.9"
